# Локальные данные Aviora Catalog

| Файл / папка | Описание |
|--------------|----------|
| `aviora_catalog_index.json` | Кэш Scan (генерируется, в git не попадает) |
| `aviora_code_snapshots/` | Снимки кода (в `CATALOG_ROOT/05data/`, см. ленту «Снимок») |

После Scan индекс пересоздаётся автоматически.
