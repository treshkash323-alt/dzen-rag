# Aviora Catalog — видение: мультифилиальная мастерская

> Для работы с проектами разных моделей и филиалов: поиск, правка, конвертация, логотипы, код, снимки для агента.

## Уже есть (MVP)

- Дерево + Scan + поиск по веткам (Cursor / Claude / AIKIVAVIORA)
- Правка `.md` / `.txt`, Word-превью, Figma-lite, фото, zip
- LLM по открытому файлу, лента, undo, снимки кода → `05data/aviora_code_snapshots/`

## Слой 1 — Снимки и «фреймворк для агента» ✅ v0.3

| Функция | Статус |
|---------|--------|
| Единый индекс SEQ в имени | ✅ |
| Мета `.aviora.meta.json` | ✅ |
| Пакетный снимок + `batches/BATCH-….json` | ✅ |
| Diff двух путей / SEQ | ✅ |
| MANIFEST + Scan | ✅ (после Scan) |

**Дальше:** side-by-side diff, теги в UI, история SEQ по исходнику.

## Слой 2 — Код и репозитории

| Функция | Зачем |
|---------|--------|
| **Просмотр code read-only** | `.py/.ts/…` в редакторе без VS Code |
| **Подсветка синтаксиса** | Prism/highlight.js — быстрее читать |
| **Открыть в Cursor** | Кнопка `cursor://` / путь в буфер |
| **Git-кратко** | Ветка, last commit, dirty — в панели свойств |
| **Клон-задача** | «Сохранить N файлов модуля» одним кликом в snapshots |

## Слой 3 — Конвертация и контент

| Функция | Зачем |
|---------|--------|
| **md → docx** (pandoc) | Сдача ДЗ, Word |
| **md → HTML/PDF** | Публикация |
| **Логотипы / assets** | Пресеты размеров, favicon, watermark batch |
| **Переименование по шаблону** | Единый стиль имён в `Dzen/` и `00docs/` |

## Слой 4 — Сравнение филиалов

| Функция | Статус |
|---------|--------|
| **Карта модулей** | ✅ вкладка Карта, `GET /catalog/map` |
| Зеркало пути + diff филиалов | TODO |
| Импорт строк из ASSETS_REGISTRY | TODO |

## Слой 5 — Ускорение рутины

| Функция | Зачем |
|---------|--------|
| **Шаблоны файлов** | New MODULE_CARD / README / .env.example из кнопки |
| **Чеклист перед сдачей** | Scan, secrets, docker, порт — галочки |
| **Быстрые фильтры** | «только изменённые за 7 дней», «только без .bak» |
| **Связка с Dzen RAG** | «Проиндексировать этот md в :8001» |
| **Очередь задач** | Список «открыть / поправить / снимок / экспорт» |
| **Пакетное переименование** | regex по выделенным в дереве |

## Слой 6 — Для работы с AI (вас + Cursor)

| Функция | Зачем |
|---------|--------|
| **Экспорт контекста** | Один zip: файл + meta + соседние импорты |
| **Промпт из шаблона** | «Найди в snapshots PROJ=aviora REV=v023» |
| **История снимков по файлу** | Все SEQ для `app.js` |
| **Рейтинг «взять в прод»** | Пометка ★ на лучшем дубликате |

## Приоритет внедрения (рекомендация)

1. ✅ Снимки + пакет + diff + карта (v0.3)
2. Просмотр code + «в Cursor» + подсветка синтаксиса
3. md→docx + пакет логотипов
4. Diff зеркал Cursor ↔ Claude
5. Очередь задач + связка RAG

---
Обновляйте этот файл по мере реализации. См. `MODULE_CARD.md` для статуса релиза.
