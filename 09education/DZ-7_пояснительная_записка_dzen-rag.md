# ДЗ-7 — пояснительная записка (промежуточная версия)

**Студент:** Игорь Кашинцев  
**Курс:** VibeCoder, Тема 7 — RAG + FastAPI + векторная база  
**Проект:** Dzen RAG — ассистент по материалам канала «Канал про ИИ»  
**Рабочая копия:** `Cursor/AIKIVAVIORA_v.3_Cursor/` (развивается дальше)  
**Архив для сдачи:** позже копия в `Cursor/Projects/ДЗ-7/` (заморозка, не трогаем)  
**Дата:** 30.05.2026  
**Статус:** MVP — ingest, API, чат, фронт; интеграция с AIKIVAVIORA — следующая фаза

> Формат по образцу: `02modules/dzen-rag/образец для дз-7 смотри здесь_ДЗ-6_отчёт_для_сдачи.docx`  
> (и markdown-версия: `Projects/ДЗ-6/GOOGLE_DOC_черновик.md`)  
> В Google Doc перед сдачей: вставить скрины из раздела 2 и экспортировать в PDF при необходимости.

---

## 1. Исходные материалы

### Канон контента (вне git)

| Источник | Путь |
|----------|------|
| База RAG на G: | `G:\3_Дзен\1G_канал про ИИ_статьи на Дзене\4_AIKIVAVIORA_база-RAG\` |
| Проиндексированный модуль | `модуль 1.3 - 24.05.2026` (слоты 1F–9F) |

### Состав модуля 1.3 (ingest)

| Тип | Кол-во | Примечание |
|-----|--------|------------|
| PDF уроков/практик | 8 | Теория и практика по ролям (Инженер, Учитель, Родитель, Помощник школьника) |
| Markdown (Playwright) | 1 | `4F_10-00_урок 3_Родитель\statiia_....md` |
| README слотов | 9 | Заглушки 1F–9F |

*Видео урока (.mp4) и сырой архив Дзена в репозиторий не включаются.*

### Методичка курса (референс, не копируем код)

| Файл | Назначение |
|------|------------|
| `Projects/ДЗ-7/материалы с заданиями/RAG_FastAPI.docx` | Архитектура TrailCamp — **использована как референс**, не как имя проекта |

---

## 2. Процесс работы в Cursor

> **Сюда вставить 5–7 скриншотов перед сдачей:**
> 1. Структура проекта `AIKIVAVIORA_v.3_Cursor` и модуль `02modules/dzen-rag`
> 2. Запуск ingest: `tools/ingest_from_g.py --reset` (вывод: 18 файлов, 126 чанков)
> 3. Swagger `/docs` — POST `/chat` с ответом DeepSeek
> 4. Фронт: http://127.0.0.1:8001/ui/ — вопрос и ответ
> 5. GET `/health` — `chunks_in_index: 126`, `llm_deepseek_configured: true`
> 6. Файл `backend/.env` (**без ключа** — замазать sk-...)
> 7. (опционально) Проводник G: — папка модуля 1.3

Промпты и команды: `02modules/dzen-rag/SHPARGALKA.md`

---

## 3. Архитектура RAG (что построено)

```
G: .../4_AIKIVAVIORA_база-RAG/
        ↓  tools/ingest_from_g.py  (.md / .txt / .pdf, pypdf)
05data/rag_index/  (ChromaDB, локально)
        ↓
02modules/dzen-rag/backend  (FastAPI)
        ↓
DeepSeek API  |  LM Studio :1234 (опционально)
        ↓
frontend /ui/  — чат в браузере
```

### Отличия от методички TrailCamp

| TrailCamp (урок 7) | Dzen RAG (этот проект) |
|--------------------|-------------------------|
| Gemini embeddings + Gemini Flash | sentence-transformers (локально) + DeepSeek / LM Studio |
| POST `/upload` — загрузка файлов в API | Ingest с диска G: (`RAG_SOURCE_PATH`) |
| React + Vite | Статический HTML/JS (`frontend/`) |
| `chroma_db/` в корне | `05data/rag_index/` |
| Название TrailCamp | Канал «Канал про ИИ», модуль AIKIVAVIORA v.3 |

---

## 4. Реализация

### Стек

- **Backend:** FastAPI, ChromaDB, sentence-transformers, pypdf, httpx  
- **Embeddings:** `paraphrase-multilingual-MiniLM-L12-v2` (локально)  
- **LLM:** DeepSeek API (основной), LM Studio (опционально)  
- **Общий код:** `aikivaviora_shared/rag/`

### API-эндпоинты

| Метод | Путь | Назначение |
|-------|------|------------|
| GET | `/health` | Статус, число чанков, флаги LLM |
| POST | `/ingest` | Переиндексация с G: |
| POST | `/chat` | Вопрос → retrieval + ответ LLM |
| GET | `/ui/` | Веб-чат |

### Результат ingest (модуль 1.3)

| Метрика | Значение |
|---------|----------|
| Файлов найдено | 18 |
| Файлов проиндексировано | 18 |
| Чанков в индексе | **126** |
| Ошибок | 0 |

Параметры чанкинга: размер 900, overlap 120 (`RAG_CHUNK_SIZE`, `RAG_CHUNK_OVERLAP`).

---

## 5. Тестирование

### Retrieval (поиск по базе)

**Вопрос:** «Что делегировать ИИ инженеру?»

- Найдены фрагменты из PDF урока 09:00 (Инженер, модуль 1.3)
- Score релевантности ~0.72–0.73
- Источники отображаются в `/chat` и во фронте `/ui/`

### Генерация ответа (DeepSeek)

- Провайдер: `auto` → `deepseek`
- `llm_status: ok`
- Ответ структурирован по контексту: жёлтая зона (можно делегировать) vs красная (нельзя)

### Пример вопросов для проверки (модуль 1.3)

1. Что делегировать ИИ инженеру?  
2. Где граница между помощью ребёнку и списыванием?  
3. Какие промпты использовать школьнику для учёбы?  
4. Какие задачи учитель может делегировать ИИ, а какие нет?

### Сложности

| Проблема | Решение |
|----------|---------|
| Порт 8000 занят Windows | Запуск на **8001** (`start-api.bat`, `run.ps1`) |
| uvicorn «Could not import app» | Запуск только из папки `backend/` |
| `no_llm_configured` при наличии ключа | Перезапуск API; порядок загрузки `.env` |
| PDF с эмодзи в имени файла | Ingest работает; в метаданных — полное имя файла |

---

## 6. Запуск (кратко)

```powershell
# Ярлык или батник:
02modules\dzen-rag\start-api.bat

# Чат:
http://127.0.0.1:8001/ui/
```

Полная инструкция: `02modules/dzen-rag/SHPARGALKA.md`

---

## 7. Архив для сдачи (план)

**Рабочий проект (git):**  
`C:\Users\kash-\Python_kash\Cursor\AIKIVAVIORA_v.3_Cursor\`

**Копия для ДЗ-7 (сделать перед сдачей, не развивать):**  
`C:\Users\kash-\Python_kash\Cursor\Projects\ДЗ-7\dzen-rag-snapshot_ДД.ММ.ГГГГ\`

**В архив включить:**
- исходники (`02modules/`, `aikivaviora_shared/`, `tools/`, `config/`, `00docs/` по необходимости)
- `09education/DZ-7_пояснительная_записка_dzen-rag.md` (этот файл)
- `SHPARGALKA.md`, `README.md`
- **не включать:** `.env`, `05data/rag_index/`, `.venv/`, ключи

**Формат сдачи курса:**
- Zip с проектом
- Google Doc: скрины из раздела 2 + выводы из разделов 5–8

---

## 8. Связь с AIKIVAVIORA (после сдачи ДЗ)

Эта копия в `AIKIVAVIORA_v.3_Cursor` — **продуктовая ветка**:

- интеграция с экосистемой AIKIVAVIORA / NEXUS  
- Telegram-бот как клиент API  
- новые модули на G: (1.4, 1.5…)  
- deploy в `08deploy/`

Карточка модуля: `02modules/dzen-rag/MODULE_CARD.md`

---

## 9. Вывод

Задание по теме 7 выполнено на уровне **рабочего MVP**: построен RAG-пайплайн (ingest PDF/md с G: → ChromaDB → FastAPI `/chat` → DeepSeek), проиндексирован модуль 1.3 канала (126 чанков), добавлен веб-интерфейс и документация для запуска.

Главный практический вывод: **RAG над своими материалами (Дзен, PDF с текстовым слоем) даёт ответы с привязкой к источникам; качество зависит от чанкинга и актуальности индекса после каждого нового выпуска.**

---

## 10. Чек-лист перед сдачей

- [ ] Скрины в Google Doc (раздел 2)
- [ ] Zip без `.env` и без индекса
- [ ] Копия проекта в `Projects/ДЗ-7/` (архив)
- [ ] Проверка `/chat` на 3–4 вопросах модуля 1.3
- [ ] Ссылка на Google Doc в форму сдачи
